/*import { Injectable } from "@angular/core";
import { Usuario } from "../usuario.model";
import { Adapter } from "./adapter.adapter";

@Injectable({
  providedIn: "root"
})
export class UsuarioAdapter implements Adapter<Usuario> {
  adapt(item: any): Usuario {
    var usuario: Usuario = new Usuario();
    usuario.id = item.id;
    usuario.rol = item.rol;
    usuario.username = item.username;
    usuario.password = item.password;
    usuario.nombre = item.nombre;
    usuario.apellido = item.apellido;
    usuario.fechaNacimiento = item.fechaNacimiento;
    usuario.email = item.email;
    usuario.genero = item.genero;
    usuario.telefono = item.telefono;
    usuario.emailConfirmado = item.emailConfirmado;
    usuario.website = item.website;
    usuario.imagen = item.imagen;
    usuario.infouser = item.infouser;

    return usuario;
  }
}*/
