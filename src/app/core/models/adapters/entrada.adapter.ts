/*import { Injectable } from "@angular/core";
import { Entrada } from "../entrada.model";
import { Adapter } from "./adapter.adapter";

@Injectable({
  providedIn: "root"
})
export class EntradaAdapter implements Adapter<Entrada> {
  adapt(item: any): Entrada {
    var entrada: Entrada = new Entrada();
    entrada.id = item.id;
    entrada.titulo = item.titulo;
    entrada.contenido = item.contenido;
    entrada.tipoEntrada = item.tipoEntrada;
    entrada.resumen = item.resumen;
    entrada.fechaPublicacion = item.fechaPublicacion;
    entrada.fechaEdicion = item.fechaEdicion;
    entrada.borrador = item.borrador;
    entrada.publicada = item.publicada;
    entrada.password = item.password;
    entrada.privado = item.privado;
    entrada.estado = item.estado;
    entrada.permitirComentario = item.permitirComentario;
    entrada.imagenDestacada = item.imagenDestacada;
    entrada.votos = item.votos;
    entrada.cantidadComentarios = item.cantidadComentarios;
    entrada.usuario = item.usuario;
    entrada.usuarioEditado = item.usuarioEditado;
    entrada.categorias = item.categorias;
    entrada.etiquetas = item.etiquetas;
    
    return entrada;
  }
}*/
