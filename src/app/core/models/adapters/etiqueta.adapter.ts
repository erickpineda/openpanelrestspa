/*import { Injectable } from "@angular/core";
import { Etiqueta } from "../etiqueta.model";
import { Adapter } from "./adapter.adapter";

@Injectable({
  providedIn: "root"
})
export class EtiquetaAdapter implements Adapter<Etiqueta> {
  adapt(item: any): Etiqueta {
    var etiqueta : Etiqueta = new Etiqueta();
    etiqueta.id = item.id;
    etiqueta.nombre = item.nombre;
    etiqueta.frecuencia = item.frecuencia;
    etiqueta.descripcion = item.descripcion;

    return etiqueta;
  }
}*/
