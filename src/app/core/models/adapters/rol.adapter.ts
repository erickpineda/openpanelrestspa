/*import { Injectable } from "@angular/core";
import { Rol } from "../rol.model";
import { Adapter } from "./adapter.adapter";

@Injectable({
  providedIn: "root"
})
export class RolAdapter implements Adapter<Rol> {
  adapt(item: any): Rol {
    var rol : Rol = new Rol();
    rol.id = item.id;
    rol.nombre = item.nombre;
    rol.descripcion = item.descripcion;

    return rol;
  }
}*/
